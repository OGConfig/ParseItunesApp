<h1>ParseItunesApp</h1>
Android App implements Parse.com API. Functions consists of a Login activity, Create Account Activity, Settings tab, and display activity. If the users don’t have an account, they must create one. Then they are directed to the preview activity. From there they can navigate to settings tab, which they can view all apps, view favorites, clear favorites, view shared, clear shared or logout. This app parses data from iTunes top grossing apps.

